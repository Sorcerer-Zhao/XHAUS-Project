# Skills（本地，不入库）

本目录用于放置带 `SKILL.md` 的技能包。仓库默认**不提交**此目录下的具体内容。

安装方式（任选）：

1. 将技能文件夹复制到本目录，例如 `xhaus/templates/skills/my-skill/SKILL.md`
2. 或放到用户目录：`~/.xhaus/skills/`
3. 或通过环境变量：`export XHAUS_SKILLS_DIR=/path/to/skills`

运行时由 `xhaus/skills/` 模块扫描上述路径。
