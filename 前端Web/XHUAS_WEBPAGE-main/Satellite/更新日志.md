### 更新日志

`main_satellite.py`:

1. 170行增加了检测`fresh`是否为空的判断，以免重复写入
2. 317行对异常检测做了修改，把三个异常分别检测，防止出现其它情况导致沙盒被清空
3. 提取`_fresh_patches()`统一计算新增补丁，LLM合并和fallback追加都只处理未重复内容
4. `_merge_one_file()`改为返回是否实际写入，重复补丁不会生成`.bak`，也不会被计入`written`

`test_main_satellite.py`:

1. 增加“补丁已存在时不重写文件、不生成`.bak`”的回归测试
